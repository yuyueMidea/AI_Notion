
# 前端日常开发高频算法详解

## 一、数据处理算法

> 日常最高频。后端吐数据，前端负责"整形"——分组、拍平、去重、排序、搜索，几乎每个页面都用到。

### 1. 扁平化 ↔ 树形转换

**核心思想**：用哈希表 `O(n)` 构建 id→节点 映射，一次遍历挂载父子关系，避免 `O(n²)` 的嵌套查找。

**使用场景**：
- 菜单/权限树：接口返回带 `parentId` 的扁平数组 → 组件需要嵌套树形结构
- 无限层级评论区构建
- 组织架构图数据
- Ant Design Tree / El-Tree 的 `data` 数据生成

```js
// O(n) 扁平列表 → 树形结构
function listToTree(list) {
  const map = Object.fromEntries(list.map(n => [n.id, { ...n, children: [] }]));
  return list.filter(n => {
    if (n.parentId) map[n.parentId]?.children.push(map[n.id]);
    return !n.parentId;
  }).map(n => map[n.id]);
}

// 树形 → 扁平（DFS）
function treeToList(tree, result = []) {
  tree.forEach(node => {
    const { children, ...rest } = node;
    result.push(rest);
    if (children?.length) treeToList(children, result);
  });
  return result;
}
```

---

### 2. 分组 / GroupBy

**核心思想**：`reduce` 或 `Object.groupBy`（ES2024），将数组按 key 分桶，复杂度 `O(n)`。

**使用场景**：
- 日历组件：事件列表按日期分组
- 账单页：按月/类别聚合统计金额
- 看板（Kanban）：任务按状态分列渲染
- ECharts 多系列数据预处理

```js
// 通用 groupBy
const groupBy = (arr, key) =>
  arr.reduce((acc, item) => {
    const k = typeof key === 'function' ? key(item) : item[key];
    (acc[k] ??= []).push(item);
    return acc;
  }, {});

// 使用
groupBy(orders, o => o.date.slice(0, 7)); // 按年月分组
```

---

### 3. 排序与多级排序

**核心思想**：浏览器内置 `Array.sort` 是 TimSort（稳定，`O(n log n)`），关键是 comparator 的设计。多级排序用短路写法链式连接。

**使用场景**：
- 表格列头点击排序（支持升降序切换）
- 搜索结果按相关性 + 时间双重排序
- 拖拽列表保存 `order` 字段后重排
- 优先队列模拟（任务调度器）

```js
// 多级排序：先按优先级降序，再按创建时间升序
tasks.sort((a, b) =>
  b.priority - a.priority || a.createdAt - b.createdAt
);

// 通用多级排序工厂
const multiSort = (...fns) => (a, b) =>
  fns.reduce((r, fn) => r || fn(a, b), 0);
```

---

### 4. 去重 / 差集 / 交集

**核心思想**：`Set` 查找 `O(1)`，集合运算整体 `O(n)`，远优于双重循环的 `O(n²)`。

**使用场景**：
- 权限系统：本次勾选 vs 原始权限 → 计算新增/删除集合
- 搜索 Tag 过滤去重合并
- 购物车多来源合并
- 表格批量操作行选中管理

```js
const a = new Set([1, 2, 3, 4]);
const b = new Set([3, 4, 5, 6]);

const union  = new Set([...a, ...b]);                     // 并集
const inter  = new Set([...a].filter(x => b.has(x)));    // 交集
const diffAB = new Set([...a].filter(x => !b.has(x)));   // a 相对 b 的差集
```

---

### 5. 字符串搜索 / 模糊匹配

**核心思想**：精确搜索用 `includes` / 正则；容错模糊搜索用 Bitap 算法（fuse.js 的实现基础）。

**使用场景**：
- Select 下拉框本地搜索过滤
- 命令面板（cmd+k）模糊搜索
- 富文本查找替换 + 高亮
- 输入联想词高亮匹配

```js
// 带高亮的正则搜索
function highlight(text, keyword) {
  const re = new RegExp(`(${keyword.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')})`, 'gi');
  return text.replace(re, '<mark>$1</mark>');
}

// 模糊匹配（使用 fuse.js）
import Fuse from 'fuse.js';
const fuse = new Fuse(list, { keys: ['name', 'label'], threshold: 0.4 });
const results = fuse.search(query);
```

---

## 二、交互控制算法

> 管理用户行为的频率与状态，核心问题是"何时触发"和"当前处于什么状态"。

### 1. 防抖 Debounce

**核心思想**：每次触发重置计时器，只有在沉默 delay 毫秒后才真正执行。

**使用场景**：
- 搜索框 `onChange` → 接口请求（避免每次击键都发请求）
- 窗口 `resize` 后重新计算布局
- 表单实时校验延迟触发
- 编辑器自动保存草稿

```js
function debounce(fn, delay) {
  let timer;
  return function (...args) {
    clearTimeout(timer);
    timer = setTimeout(() => fn.apply(this, args), delay);
  };
}

// 带 immediate 立即执行版
function debounce(fn, delay, immediate = false) {
  let timer;
  return function (...args) {
    const callNow = immediate && !timer;
    clearTimeout(timer);
    timer = setTimeout(() => { timer = null; if (!immediate) fn.apply(this, args); }, delay);
    if (callNow) fn.apply(this, args);
  };
}
```

---

### 2. 节流 Throttle

**核心思想**：固定时间窗口内只执行一次，不丢弃过程（与防抖的根本区别）。

**使用场景**：
- 页面滚动进度条实时更新
- 鼠标拖拽坐标实时计算
- Canvas 绘制路径的坐标采样
- 按键连射频率控制

```js
// 时间戳版（立即执行，尾部可能丢失）
function throttle(fn, interval) {
  let last = 0;
  return function (...args) {
    const now = Date.now();
    if (now - last >= interval) {
      last = now;
      fn.apply(this, args);
    }
  };
}

// RAF 版（对齐帧率，适合动画场景）
function throttleByRaf(fn) {
  let rafId;
  return function (...args) {
    if (rafId) return;
    rafId = requestAnimationFrame(() => {
      fn.apply(this, args);
      rafId = null;
    });
  };
}
```

---

### 3. 有限状态机 FSM

**核心思想**：枚举所有合法状态 + 转移事件，非法状态组合在结构上就不存在，彻底杜绝 `isLoading && isSuccess && isError` 这类混乱布尔值。

**使用场景**：
- 多步骤表单向导（`idle → filling → validating → submitting → done/error`）
- 媒体播放器状态（`idle/loading/playing/paused/ended/error`）
- 拖拽组件（`idle/dragging/over/dropped`）
- 异步请求状态管理（取代多个 boolean flag）

```js
const machine = {
  idle:       { FETCH: 'loading' },
  loading:    { SUCCESS: 'success', FAIL: 'error' },
  success:    { RESET: 'idle' },
  error:      { RETRY: 'loading', RESET: 'idle' },
};

function createFSM(initial) {
  let state = initial;
  return {
    send(event) {
      state = machine[state]?.[event] ?? state;
      return state;
    },
    getState: () => state,
  };
}
```

---

### 4. 请求并发控制

**核心思想**：维护一个「正在运行」的 Promise 池，超过并发限制时等待最先完成的请求腾出位置。

**使用场景**：
- 大文件分片并发上传（限制 3 并发）
- 批量图片预加载
- 大表格批量操作接口调用
- 任何需要限速的批量异步任务

```js
async function pLimit(tasks, concurrency = 3) {
  const results = new Array(tasks.length);
  const pool = new Set();

  for (let i = 0; i < tasks.length; i++) {
    const idx = i;
    const p = tasks[i]().then(r => {
      results[idx] = r;
      pool.delete(p);
    });
    pool.add(p);
    if (pool.size >= concurrency) await Promise.race(pool);
  }

  await Promise.all(pool);
  return results;
}
```

---

### 5. 撤销/重做（双栈历史）

**核心思想**：`undoStack` 存操作历史，`redoStack` 存撤销的操作；新操作时清空 redoStack。结合 Immer 不可变数据使实现大幅简化。

**使用场景**：
- 富文本编辑器 Ctrl+Z / Ctrl+Y
- 可视化设计工具（拖拽、属性修改）
- 绘图板笔迹撤销
- 表单步骤回退

```js
class History {
  constructor(initial) {
    this.undoStack = [];
    this.redoStack = [];
    this.current = initial;
  }
  push(newState) {
    this.undoStack.push(this.current);
    this.redoStack = []; // 新操作清空重做栈
    this.current = newState;
  }
  undo() {
    if (!this.undoStack.length) return;
    this.redoStack.push(this.current);
    this.current = this.undoStack.pop();
    return this.current;
  }
  redo() {
    if (!this.redoStack.length) return;
    this.undoStack.push(this.current);
    this.current = this.redoStack.pop();
    return this.current;
  }
}
```

---

## 三、树 / 图遍历

> DOM、组件树、路由、菜单、权限，前端世界本质上全是树。

### 1. DFS 深度优先遍历

**使用场景**：
- 菜单树递归渲染
- 权限树查找节点
- 文件目录树过滤搜索
- 深拷贝、深比较对象

```js
// 递归版
function dfs(node, visitor) {
  visitor(node);
  node.children?.forEach(child => dfs(child, visitor));
}

// 迭代版（显式栈，避免深层递归栈溢出）
function dfsIterative(root, visitor) {
  const stack = [root];
  while (stack.length) {
    const node = stack.pop();
    visitor(node);
    // 注意：反转子节点入栈以保持左→右顺序
    if (node.children) stack.push(...[...node.children].reverse());
  }
}
```

---

### 2. BFS 广度优先遍历

**使用场景**：
- 按 DOM 顺序收集所有可聚焦元素
- 思维导图按层展开/折叠
- 扁平化树时保留层级 depth 信息

```js
function bfs(root, visitor) {
  const queue = [{ node: root, depth: 0 }];
  while (queue.length) {
    const { node, depth } = queue.shift();
    visitor(node, depth);
    node.children?.forEach(c => queue.push({ node: c, depth: depth + 1 }));
  }
}
```

---

### 3. 路径查找 / 面包屑生成

**使用场景**：
- 侧边菜单：根据当前路由高亮并 `expand` 父级
- 面包屑导航自动生成
- 树形选择器回显祖先链

```js
function findPath(tree, targetId) {
  function dfs(nodes, path) {
    for (const node of nodes) {
      const current = [...path, node];
      if (node.id === targetId) return current;
      if (node.children?.length) {
        const found = dfs(node.children, current);
        if (found) return found;
      }
    }
    return null;
  }
  return dfs(tree, []) ?? [];
}

// 用法
const crumbs = findPath(menuTree, activeId).map(n => n.label);
// → ['首页', '系统设置', '权限管理']
```

---

### 4. 虚拟 DOM Diff

**核心思想**：同层对比 + `key` 复用，将 `O(n³)` 的树 diff 优化到 `O(n)`。理解这个是写好 React/Vue 列表的基础。

**关键原则**：
- `key` 是告诉框架「这两个节点是同一个身份」的标识
- 没有 `key` 或用 `index` 做 `key` 会导致错误复用
- 类型不同的节点直接替换，不做深度比较

```js
// 简化示意：同层列表 diff
function diffList(oldList, newList) {
  const oldMap = new Map(oldList.map(n => [n.key, n]));
  return newList.map(newNode => {
    const oldNode = oldMap.get(newNode.key);
    return oldNode
      ? { type: 'update', old: oldNode, new: newNode }
      : { type: 'create', node: newNode };
  });
}
```

---

## 四、性能优化策略

> 核心目标：减少主线程工作量，让帧率稳定在 60fps。

### 1. 虚拟列表 (Virtual Scroll)

**核心思想**：只渲染可视区域的 DOM 节点，其余用总高度撑开容器。DOM 节点数从 `O(n)` 降为 `O(可视行数)`。

**使用场景**：
- 万行数据表格（不分页方案）
- 聊天记录长列表
- 大型 Select 下拉

```js
// 固定行高版核心计算
function useVirtualList({ data, rowHeight, viewHeight, scrollTop }) {
  const total = data.length;
  const startIndex = Math.floor(scrollTop / rowHeight);
  const endIndex   = Math.min(total, Math.ceil((scrollTop + viewHeight) / rowHeight));
  const offsetY    = startIndex * rowHeight;
  const totalHeight = total * rowHeight;
  const visibleItems = data.slice(startIndex, endIndex);
  return { visibleItems, offsetY, totalHeight };
}
```

---

### 2. 记忆化 Memoization

**使用场景**：
- `useMemo` 缓存复杂列表过滤/排序计算
- `useCallback` 缓存事件处理函数，避免子组件无效 re-render
- `React.memo` 包裹纯展示组件

```js
// 通用记忆化（WeakMap 版，支持对象 key 且不影响 GC）
function memoizeWeak(fn) {
  const cache = new WeakMap();
  return (obj, ...args) => {
    if (!cache.has(obj)) cache.set(obj, fn(obj, ...args));
    return cache.get(obj);
  };
}

// React 中的典型用法
const filteredList = useMemo(
  () => list.filter(item => item.status === activeTab),
  [list, activeTab]
);
```

---

### 3. LRU 缓存淘汰

**核心思想**：`Map` 的键有插入顺序，删除后重新 `set` 可将其移到末尾；头部（`keys().next()`）即是「最久未使用」。整体 `O(1)`。

**使用场景**：
- Vue `keep-alive` 的 `max` 实现原理
- 前端接口响应本地缓存（带容量上限）
- 图片资源缓存池

```js
class LRU {
  constructor(capacity) {
    this.cap = capacity;
    this.cache = new Map();
  }
  get(key) {
    if (!this.cache.has(key)) return undefined;
    const val = this.cache.get(key);
    this.cache.delete(key);
    this.cache.set(key, val); // 移到末尾（最近使用）
    return val;
  }
  set(key, val) {
    if (this.cache.has(key)) this.cache.delete(key);
    else if (this.cache.size >= this.cap)
      this.cache.delete(this.cache.keys().next().value); // 淘汰头部
    this.cache.set(key, val);
  }
}
```

---

### 4. 时间切片 Time Slicing

**核心思想**：将大任务分成小块，用 `requestIdleCallback` 或 `MessageChannel` 在浏览器空闲帧插入执行，避免长任务（>50ms）阻塞主线程。这是 React Fiber 的核心思想。

**使用场景**：
- 前端解析大数据（万条 CSV 导入）
- Canvas 大量批量绘制分批渲染
- Web Worker 不可用时的兜底方案

```js
function runInChunks(items, process, chunkSize = 200) {
  let index = 0;
  function runChunk(deadline) {
    while (index < items.length) {
      // 优先检查剩余帧时间（requestIdleCallback 提供）
      if (deadline && deadline.timeRemaining() < 1) break;
      process(items[index++]);
    }
    if (index < items.length) {
      'requestIdleCallback' in window
        ? requestIdleCallback(runChunk)
        : setTimeout(() => runChunk(null), 0);
    }
  }
  requestIdleCallback
    ? requestIdleCallback(runChunk)
    : setTimeout(() => runChunk(null), 0);
}
```

---

## 五、工程化 / 依赖处理

> 这类算法前端工程师未必手写，但理解它们是排查构建问题、写 Babel 插件的基础。

### 1. 拓扑排序

**核心思想**：Kahn 算法（BFS）或 DFS 后序。处理 DAG（有向无环图）的执行顺序，保证「依赖先于被依赖者执行」。

**使用场景**：
- npm/pnpm 包安装顺序
- Webpack 模块打包顺序
- CI/CD 任务依赖流水线
- 电子表格单元格公式求值顺序

```js
// Kahn 算法
function topoSort(graph) {
  const inDegree = {};
  for (const [node, deps] of Object.entries(graph)) {
    inDegree[node] ??= 0;
    deps.forEach(dep => { inDegree[dep] = (inDegree[dep] ?? 0) + 1; });
  }
  const queue = Object.keys(inDegree).filter(n => inDegree[n] === 0);
  const order = [];
  while (queue.length) {
    const node = queue.shift();
    order.push(node);
    graph[node]?.forEach(dep => {
      if (--inDegree[dep] === 0) queue.push(dep);
    });
  }
  return order; // 若 order.length < 节点数，说明存在环
}
```

---

### 2. 循环依赖检测

**核心思想**：DFS 时维护「当前递归栈」，若访问到栈中已有节点则找到环。

**使用场景**：
- `eslint-plugin-import` 的 `no-cycle` 规则
- 自研模块系统循环检测
- Vite/Rollup 循环模块警告原理

```js
function detectCycles(graph) {
  const visited = new Set(), inStack = new Set();
  const cycles = [];

  function dfs(node, path) {
    visited.add(node);
    inStack.add(node);
    for (const neighbor of (graph[node] ?? [])) {
      if (inStack.has(neighbor)) {
        // 找到环，截取环的部分
        const cycleStart = path.indexOf(neighbor);
        cycles.push(path.slice(cycleStart).concat(neighbor));
      } else if (!visited.has(neighbor)) {
        dfs(neighbor, [...path, neighbor]);
      }
    }
    inStack.delete(node);
  }

  Object.keys(graph).forEach(n => {
    if (!visited.has(n)) dfs(n, [n]);
  });
  return cycles;
}
```

---

### 3. Semver 版本比较

**使用场景**：
- 理解 `package.json` 中 `^` vs `~` 的语义
- CI 自动检测 breaking change 升级
- 组件库版本兼容性判断

```js
// 解析 semver 字符串
function parseSemver(ver) {
  const [main, pre] = ver.replace(/^[^0-9]*/, '').split('-');
  const [major, minor, patch] = main.split('.').map(Number);
  return { major, minor, patch, pre };
}

function compareSemver(a, b) {
  const pa = parseSemver(a), pb = parseSemver(b);
  return pa.major - pb.major
    || pa.minor - pb.minor
    || pa.patch - pb.patch;
}

// ^1.2.3 的语义：>= 1.2.3 && < 2.0.0
function satisfiesCaret(installed, required) {
  const i = parseSemver(installed), r = parseSemver(required);
  return i.major === r.major && compareSemver(installed, required) >= 0;
}
```

---

### 4. AST 遍历 / 代码转换

**核心思想**：源码 → Parser → AST（树形结构） → 遍历 Visitor → 修改节点 → Generate → 新代码。本质是带 visitor 模式的 DFS。

**使用场景**：
- Babel 插件（自动导入、语法降级、宏展开）
- ESLint 自定义规则
- Codemod 批量重构（如 API 迁移）
- IDE 语义分析（引用查找、重命名）

```js
// Babel 插件示例：自动移除 console.log
export default function removeConsoleLogs({ types: t }) {
  return {
    visitor: {
      CallExpression(path) {
        const { callee } = path.node;
        if (
          t.isMemberExpression(callee) &&
          t.isIdentifier(callee.object, { name: 'console' }) &&
          t.isIdentifier(callee.property, { name: 'log' })
        ) {
          path.remove();
        }
      }
    }
  };
}

// 使用 @babel/parser + traverse 手动遍历 AST
import { parse } from '@babel/parser';
import traverse from '@babel/traverse';

const ast = parse(code, { sourceType: 'module' });
traverse(ast, {
  ImportDeclaration(path) {
    console.log('发现 import:', path.node.source.value);
  }
});
```

---

## 总结对照表

| 算法类别 | 代表算法 | 频率 | 典型场景 |
|---|---|---|---|
| 数据处理 | 扁平↔树转换、GroupBy、集合运算 | ⭐⭐⭐⭐⭐ | 菜单渲染、表格聚合、权限系统 |
| 交互控制 | 防抖、节流、状态机、并发控制 | ⭐⭐⭐⭐⭐ | 搜索框、拖拽、异步状态管理 |
| 树/图遍历 | DFS、BFS、路径查找、VDOM Diff | ⭐⭐⭐⭐ | 菜单高亮、面包屑、列表 key |
| 性能优化 | 虚拟列表、Memoization、LRU、时间切片 | ⭐⭐⭐⭐ | 大列表、重计算缓存、Fiber |
| 工程化 | 拓扑排序、循环检测、AST 遍历 | ⭐⭐⭐ | 构建工具、Babel 插件、ESLint |
