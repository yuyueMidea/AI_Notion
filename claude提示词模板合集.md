# 给 Claude 的提示词模板合集（按开发场景）

> 核心结构统一为【需求】【约束】【期望格式】三段，根据场景增减字段。直接复制对应模板，填空即可。

---

## 1. 架构设计 / 技术方案

```
【背景】
项目是什么、当前阶段（从0到1 / 已有系统改造）、团队规模（影响方案复杂度取舍）

【需求】
要解决的核心问题是什么，不要只说"做一个XX系统"，
而要说清楚关键场景，比如：
- 预期QPS/数据量级
- 核心业务流程（按时间顺序列出关键步骤）
- 必须支持的功能点 vs 锦上添花的功能点（用Must/Nice to have区分）

【约束】
- 技术栈限制（现有技术栈、团队熟悉度、是否允许引入新组件）
- 性能/成本要求（延迟、吞吐、预算上限）
- 时间线（影响方案是"求稳"还是"求快"）
- 不能做的事（比如不能引入新中间件、必须兼容老接口）

【期望格式】
- 先给2-3个候选方案的优劣对比（表格形式），再给推荐方案
- 推荐方案要包含：架构图描述、关键组件职责、数据流向、潜在风险点
- 不要直接给代码，先确认方案再细化
```

**追问技巧**：方案出来后，针对你最不放心的一个环节单独追问，例如「这个方案在XX组件挂掉时怎么降级？」，比让Claude一次性穷举所有边界情况效果更好。

---

## 2. 完整代码实现

```
【需求】
功能描述：输入是什么、输出是什么、核心逻辑用自然语言描述一遍
（哪怕你心里已经有实现思路，也用自然语言过一遍，避免Claude按字面意思跑偏）

【约束】
- 语言/框架/版本（精确到次版本号，比如 Python 3.11 + FastAPI 0.110）
- 代码风格（是否要type hint、是否要docstring、命名规范是否有团队约定）
- 依赖限制（只能用标准库 / 项目里已有哪些库可以复用）
- 性能要求（是否要考虑并发、大数据量下是否要流式处理）
- 错误处理要求（要不要自定义异常、日志怎么打）

【期望格式】
- 完整可运行代码，不要省略片段（不要写"// 这里实现XX逻辑"占位）
- 关键逻辑加注释，但不要逐行注释
- 代码后面用3-5条bullet说明设计取舍（为什么这么实现，而不是另一种实现方式）
- 如果有需要补充的测试用例或边界情况，单独列出
```

**避坑提醒**：如果是改已有代码，把相关的现有代码贴进来再提需求，比单纯描述"现在有一个XX函数"效果好得多——Claude看着实际代码风格写出来的东西，和你现有代码的契合度会高很多。

---

## 3. Bug 排查 / 调试

```
【现象】
- 预期行为是什么，实际行为是什么（一定要分开写，不要混在一起描述）
- 复现步骤（能稳定复现还是偶发）
- 报错信息/日志（完整贴出来，不要只摘一句"报错了"）

【背景】
- 相关代码片段（贴出报错位置上下文，不用贴整个文件）
- 最近有没有改动过相关代码（怀疑是哪次改动引入的）
- 运行环境（本地/测试/生产，有没有环境差异）

【约束】
- 排查方向上有没有已经排除的可能性（避免Claude重复你已经验证过无效的思路）
- 修复时是否要考虑历史兼容性（不能破坏其他调用方）

【期望格式】
- 先给出最可能的1-2个原因和判断依据，不要一上来列十种可能性
- 给出验证步骤（怎么确认是不是这个原因）
- 确认后再给修复方案，修复方案附带"这个改动会不会有副作用"的说明
```

---

## 4. 代码重构

```
【需求】
- 重构目标：是为了可读性、可测试性、性能，还是为了方便后续扩展某个功能
  （目标不同，重构方向完全不同，一定要写清楚）
- 当前代码的问题（哪里难维护、哪里耦合太重）

【约束】
- 行为不能变（重构不改变外部接口/返回值，这条几乎是默认约束，但建议明确写出来）
- 改动范围限制（只重构这一个模块，还是可以牵涉调用方）
- 是否要分步骤改（一次性重构 vs 分多个可独立提交的小步骤）

【期望格式】
- 先给重构思路和拆分方案，不要直接甩重构后的全部代码
- 按步骤给代码，每步说明改了什么、为什么这么改
- 标注哪些地方需要你手动跑测试验证
```

---

## 5. 代码评审 / Review

```
【背景】
- 代码用途、调用频率（决定review的严格程度，核心链路 vs 内部小工具）
- 是谁写的（自己写的/同事写的，影响反馈语气没关系，但决定评审重点）

【需求】
请从以下维度评审（按需勾选，不要让Claude漫无目的全方位审查）：
- [ ] 逻辑正确性 / 边界情况
- [ ] 性能问题
- [ ] 安全隐患
- [ ] 代码风格/可读性
- [ ] 是否有更简洁的实现方式

【期望格式】
- 按严重程度分级（必须改 / 建议改 / 风格偏好），不要混在一起列
- 每条问题给出具体行号或代码片段引用，不要泛泛而谈
- 不需要的维度明确说"不用评审xxx"，节省篇幅
```

---

## 6. 技术选型 / 工具对比

```
【需求】
- 要解决的具体问题（不是"选个数据库"，而是"需要支持地理位置查询+10万级写入QPS"）
- 候选范围（已经锁定2-3个候选，还是完全开放选型）

【约束】
- 团队现有技术栈和运维能力（决定能不能引入新东西）
- 长期维护考虑（社区活跃度、是否要考虑被收购/停更风险）
- 成本上限

【期望格式】
- 表格对比关键维度（不要写成大段文字）
- 明确给推荐结论，并说明"在什么情况下会选另一个选项"
  （比如"如果你们后续要上规模到XX量级，建议选B而不是A"）
```

---

## 通用技巧（不限场景）

1. **先给背景，再提需求**：Claude对"为什么要做这件事"的理解，直接影响它给出方案的合理性，光甩需求不给背景容易跑偏。
2. **明确"不要做什么"**：比起列出所有要求，明确排除项往往更高效，比如"不要引入新依赖""不要改动数据库schema"。
3. **分步骤要求输出**：复杂任务先让它给思路/大纲，确认后再要完整实现，比一次性要求"设计+实现+测试全给我"成功率高。
4. **附上失败案例**：如果之前问过类似问题但回答不满意，把不满意的点直接说出来（"上次给的方案没考虑并发安全"），比重新换一种问法更精准。
