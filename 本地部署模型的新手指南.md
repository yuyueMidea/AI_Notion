# 本地部署大模型指南（新手向）

本地部署模型，简单说就是把 AI 模型下载到你自己的电脑上运行，不依赖云服务。下面从最简单到稍复杂的方案依次介绍。

---

## 🥇 方案一：Ollama（最推荐新手）

**Ollama** 是目前最简单的本地部署工具，一行命令就能跑起来。

### 安装步骤

1. 去 [ollama.com](https://ollama.com) 下载安装包（支持 macOS / Windows / Linux）
2. 安装完成后，打开终端运行：

```bash
# 下载并运行 Llama3 模型（约 4GB）
ollama run llama3

# 或者跑更轻量的模型
ollama run qwen2.5:3b    # 阿里通义千问，中文效果好
ollama run gemma3:4b     # Google 的模型
```

3. 直接在终端就能对话了！

### 用 API 调用（像调用 ChatGPT 一样）

Ollama 启动后会在本地开一个接口，你可以用代码调用：

```python
import requests

response = requests.post("http://localhost:11434/api/chat", json={
    "model": "llama3",
    "messages": [{"role": "user", "content": "你好！"}],
    "stream": False
})
print(response.json()["message"]["content"])
```

---

## 🥈 方案二：配上可视化界面（Open WebUI）

光有命令行不够直观？可以给 Ollama 套一个网页界面：

```bash
# 用 Docker 一键启动（需要先装 Docker）
docker run -d -p 3000:8080 \
  -v open-webui:/app/backend/data \
  --add-host=host.docker.internal:host.gateway \
  ghcr.io/open-webui/open-webui:main
```

然后打开浏览器访问 `http://localhost:3000`，就有一个和 ChatGPT 一模一样的界面了。

---

## 🥉 方案三：LM Studio（图形界面，不用命令行）

如果你完全不想碰终端，可以用 **LM Studio**：

1. 去 [lmstudio.ai](https://lmstudio.ai) 下载
2. 在界面里搜索模型、一键下载
3. 点击启动，完全图形化操作

---

## 硬件要求参考

| 模型规模 | 推荐内存/显存 | 典型模型 |
|---|---|---|
| 3B 以下 | 4GB RAM | Qwen2.5:1.5b |
| 7B | 8GB RAM | Llama3:8b, Qwen2.5:7b |
| 14B | 16GB RAM | Qwen2.5:14b |
| 32B+ | 32GB+ / 独立显卡 | Qwen2.5:32b |

> 💡 **没有独立显卡也没关系**，用 CPU 也能跑，只是速度慢一些。M 系列 Mac 因为内存带宽高，体验特别好。

---

## 选哪个模型？

作为新手，推荐几个中文效果好的小模型：

- **Qwen2.5:7b** — 阿里出品，中文理解强，`ollama run qwen2.5:7b`
- **Llama3.1:8b** — Meta 出品，英文为主，通用能力强
- **DeepSeek-R1:7b** — 推理能力强，适合需要思考的任务

---

## 总结路线图

```
新手入门  →  安装 Ollama  →  ollama run qwen2.5:7b  →  跑通了！
                ↓
         想要好看界面  →  装 Open WebUI
                ↓
         想用代码调用  →  用 Ollama 的 API 接口
                ↓
         想更精细控制  →  学习 llama.cpp / vLLM（进阶）
```

